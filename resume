server {
    listen 5000 default_server;
    location / {
        alias /vagrant/;
        autoindex on;
        expires max;
    }
    location  /resume {
        alias /vagrant/;
        autoindex on;
        expires max;
    }
}
